<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @election.name %>
</p>

<% @election.races.each do |race| %>
  <p>
    <b>Race:</b>
    <%= link_to race.position, election_race_path(@election, race) %>
  </p>
<% end -%>

<%= link_to 'Add Race', new_election_race_path(@election) %> |
<%= link_to 'Edit', edit_election_path(@election) %> |
<%= link_to 'Back', elections_path %>
